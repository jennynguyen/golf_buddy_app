<section class="main">
  <h1>Editing Golf Event</h1>

  <%= form_for(@golf_event) do |f| %>
  <% if @golf_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@golf_event.errors.count, "error") %> prohibited this golf event from being saved:</h2>

      <ul>
      <% @golf_event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>


  <%= link_to 'Show', @golf_event %> |
  <%= link_to 'Back', golf_events_path %>
</section>
